[database]
host = "127.0.0.1"
port = 5432
user = "postgres"
password = "postgrespassword"
name = "dsp_seeds"

[performance]
compression = true
# If 0, will use number of CPU cores
generator_threads = 0
# Number of writer threads (database connections)
writer_threads = 8
batch_size = 500
channel_capacity = 20000
